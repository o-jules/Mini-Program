<template>
    <view class="counter">
        <button @tap="plus" size="mini"> + </button>
        <button @tap="minus" size="mini"> - </button>
        <text class="count" :class="countStatus"> {{num}} </text>
    </view>
</template>

<script lang="typescript" src="./index.ts"></script>
<style lang="less">
.counter {
  text-align: left;
  font-size: 12px;
}
.count {
  font-size: 18px;
  font-weight: bold;
  &.red {
    color: red;
  }
  &.green {
    color: green;
  }
}
</style>
