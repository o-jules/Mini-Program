<style lang="less">
  .counter {
    text-align: left;
    font-size: 12px;
  }
  .count {
    font-size: 18px;
    font-weight: bold;
    &.red {
      color: red;
    }
    &.green {
      color: green;
    }
  }
</style>

<template lang="jade">
  view.counter
    button(@tap="plus", size="mini") +
    button(@tap="minus", size="mini") -
    text.count(:class="{red: num > 55, green: num < 45}") {{num}}
</template>

<script lang="typescript" src="./index.ts"></script>
